1.全量数据存储在基于磁盘的数据库，查询很慢
2.全量数据存储与基于内存的数据库，造价很贵
3.折中一下，因为实际场景，用户指挥查询部分用户关心的数据，所以我们仅将
  用户关心的数据存储与redis，既解决了查询慢又能够降低成本


扩展知识：为什么redis不支持sql，仅支持 kv查询
4.因为redis是存储非全量数据的数据库，一旦设计成sql性数据库，就会引发sql数据库的
  3范式的约束，例如：表里面的字段需要另一张表来提供，需要这种传递依赖。而恰巧传递
  依赖的表，在redis的缓存中被剔除了，就很难保证数据的准确性。所以redis被设计成了KV形式
  的数据库